namespace ws{
// who should own this??
// what should the return type be 
// i don't want to allocated on the heap 
// well i can alway just split a big message with
// fragmentatoin 
// or just multiple frames
// but how about for reading???

// this is what is understand from the rfc about sending data:
//
// here is what i will think now 
// i will have a class 
// websocket client 
// the client hold 
// the client will perform the handshake ???
// first i will try to just send data the receive later 
// the client will expose a send api 
// i don't like the config approach for boost i will preferably have two 
// overloaded fuctions for binary and text
// about fragmentation ?
// beast exposes two api 
// one where it send and 8k full frame(fin=1)
// and the other where it has fin=0 until the client closes it 
// i want my client to be agnositque to such a thing 
// but how do servers like nginx handle infinite streams??
// how will i handle infinite stream?
} // namespace ws::client
